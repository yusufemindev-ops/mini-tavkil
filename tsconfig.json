{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "//": "Excluded: `admin` is a separate package with its own tsconfig and its own `@/*` root, so checking it here resolves its imports against this app's src (use `pnpm admin:typecheck`). `worker.ts` imports .open-next/worker.js, which the OpenNext build generates AFTER `next build` — checking it here is a chicken-and-egg failure, and wrangler bundles it at deploy time.",
  "exclude": ["node_modules", "admin", "public", "worker.ts"]
}
